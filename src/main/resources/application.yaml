server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bankcore
    driver-class-name: org.postgresql.Driver
    username: bankcore
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

springdoc:
  api-docs:
    version: openapi_3_0